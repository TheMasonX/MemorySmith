# Python dependencies for MemorySmith training harness (batched QLoRA variant)
# Audit finding: TRAIN-004 / High — no requirements.txt (Audit #6 / #9).
#
# Installation
# ------------
# For GPU (CUDA 12.x) training — install a CUDA-compatible torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# For CPU-only or simulated-mode testing:
#   pip install -r requirements.txt
#
# Minimum Python version: 3.10

# Core training stack
torch>=2.3.0,<3.0.0
transformers>=4.40.0,<5.0.0
peft>=0.10.0,<2.0.0
accelerate>=0.29.0,<2.0.0

# QLoRA / 4-bit quantisation
# Required when loadIn4Bit=true (the default). The harness gracefully falls back
# to bf16/fp16 loading with a logged event if bitsandbytes is absent.
bitsandbytes>=0.43.0
